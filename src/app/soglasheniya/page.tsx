import type { Metadata } from "next";
import { PageHero } from "@/components/page-hero";
import { pageMetadata } from "@/lib/seo";

export const metadata: Metadata = pageMetadata(
  "Соглашения и конфиденциальность",
  "Правила обработки данных и использования сайта Клиника в цифрах.",
  "/soglasheniya",
);
const sections = [
  [
    "Пользовательское соглашение",
    "Материалы сайта носят информационный характер. Отправка формы не создаёт обязательств заключить договор и не является публичной офертой.",
  ],
  [
    "Обработка персональных данных",
    "Данные из формы используются только для связи по указанной задаче, подготовки предварительного разбора и организации дальнейшего взаимодействия.",
  ],
  [
    "Какие данные передаются",
    "Имя, компания или клиника, контакт, сайт, комментарий, страница отправки, источник, UTM-метки и дата обращения.",
  ],
  [
    "Передача заявки",
    "Заявка может быть передана через защищённый серверный обработчик в рабочий Telegram-чат команды и в дальнейшем — в выбранную CRM.",
  ],
  [
    "Файлы cookie и аналитика",
    "Сайт может использовать технические cookie, Яндекс Метрику и UTM-метки для анализа работы страниц и источников обращений.",
  ],
  [
    "Уточнение юридических данных",
    "До production-публикации заказчик должен предоставить полное наименование оператора данных, реквизиты и контакт для обращений по персональным данным.",
  ],
];
export default function Page() {
  return (
    <>
      <PageHero
        path="/soglasheniya"
        eyebrow="Юридическая информация"
        title="Соглашения и конфиденциальность"
        lead="Черновая структура страницы. Реквизиты оператора данных необходимо добавить до запуска на основном домене."
        actions={false}
      />
      <section className="section legal-section">
        <div className="container legal-grid">
          <aside>
            {sections.map(([title], i) => (
              <a href={`#legal-${i + 1}`} key={title}>
                {title}
              </a>
            ))}
          </aside>
          <div>
            {sections.map(([title, text], i) => (
              <article id={`legal-${i + 1}`} key={title}>
                <span>0{i + 1}</span>
                <h2>{title}</h2>
                <p>{text}</p>
              </article>
            ))}
          </div>
        </div>
      </section>
    </>
  );
}
